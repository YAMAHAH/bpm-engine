{
  "name": "bpm-engine-persist-mongoose",
  "version": "1.1.13",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src --copy-files --out-dir dist/",
    "watch": "babel src --watch --copy-files --out-dir dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bpm",
    "persist",
    "mongodb",
    "mongoose"
  ],
  "dependencies": {
    "bpm-engine": "^1.1.13",
    "mongoose": "^5.2.9",
    "source-map-support": "^0.5.12"
  },
  "devDependencies": {
    "babel-runtime": "^6.26.0",
    "bluebird": "^3.5.4"
  }
}
