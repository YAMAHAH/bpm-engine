{
  "name": "bpm-engine",
  "version": "1.0.12",
  "description": "Business Process Management Engine for JavaScript",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean:dist && babel src --copy-files --out-dir dist/",
    "watch": "npm run clean:dist && babel src --watch --copy-files --out-dir dist/",
    "browserify": "browserify index.js -o browser/engine.js -s BPMEngine",
    "clean:dist": "del 'dist/**/!(.gitkeep)'",
    "clean:browser": "del 'browser/**/!(.gitkeep)'",
    "uglify": "uglifyjs --compress --mangle -o browser/engine.min.js -- browser/engine.js",
    "build:browser": "npm run clean:browser && npm run build && npm run browserify && npm run uglify"
  },
  "files": [
    "dist/",
    "browser/",
    "index.js"
  ],
  "keywords": [
    "bpm",
    "workflow",
    "engine"
  ],
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "bpmn-moddle": "2",
    "debug": "^3.1.0",
    "iso8601-repeating-interval": "^0.1.4",
    "jexl": "^1.1.4",
    "shortid": "^2.2.8",
    "source-map-support": "^0.5.0"
  }
}
